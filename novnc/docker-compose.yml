services:
  novnc:  
    build:
      context: .
      args:
        BUILD_FROM: debian:bullseye
    image: novnc
    environment:
      # Adjust to your screen size
      - DISPLAY_WIDTH=1600
      - DISPLAY_HEIGHT=968
    ports:
      - "8080:8080"
